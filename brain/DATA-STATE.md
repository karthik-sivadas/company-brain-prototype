# Data state

_Generated by `brain sync` / `brain setup`. Lists what is configured against what has
actually been extracted. If a stream is configured but not synced, the data can be
pulled — see "When the data you need is not there" in AGENT-BRIEF.md._

## github

Scope: owner=polymetrics-ai · repo=cli · auth_type=public · rate_limit_ip=127.0.0.1 · max_pages=1

| stream | table | synced |
|---|---|---|
| issues | gh_issues | yes — 46 rows |
| pull_requests | github_pull_requests | yes — 100 rows |

Also available from this connector, not extracted yet (35): repository, branches, commits, tags, releases, labels, milestones, issue_comments, pull_request_review_comments, collaborators, contributors, stargazers, subscribers, workflows, workflow_runs, workflow_artifacts, deployments, commit_comments, deploy_keys, webhooks, environments, forks, invitations, issue_events, code_scanning_alerts, dependabot_alerts, secret_scanning_alerts, security_advisories, repo_rulesets, autolinks, languages, projects, project_items, discussions, discussion

These use credentials that are already configured, so they can be requested the same way — see "Requesting extraction" below. Adding a *new connector* is different: that needs credentials and is a host change.

## Requesting extraction

Write `/workspace/requests/sync.json`:

```json
{"connection": "<connection name>", "streams": ["<stream>"], "reason": "<why>"}
```

The host runs it when your turn ends and reports back in the same thread.
